# Timeline Workspace — 최종 인수인계 문서

> 목적: 기존 Timeline Workspace 개발을 Claude Code로 이어가기 위한 PRD + 기능 명세 + 데이터 모델 + UI/UX 규칙 + Excel Export 규칙 + 구현 원칙을 한 문서로 정리한다.
>
> 기준일: 2026-08-20
> 문서 상태: 개발 인수인계용 최종 설계안
> 중요: 아래 규칙은 현재 대화에서 합의된 요구사항을 기준으로 하며, 임의로 단순화하거나 기존 요구를 제거하지 않는다.

---

# 1. 제품 개요

## 1.1 제품명

Timeline Workspace / Scheduler

## 1.2 핵심 목적

프로젝트와 업무를 계층적으로 관리하면서, 각 업무의 일정을 Timeline/Gantt 형태로 시각화하는 작업 일정 관리 도구.

핵심 특징:

- 프로젝트 → 하위 프로젝트 → 업무의 무제한 계층 구조
- 하루 단위 Timeline Grid
- 업무/프로젝트의 직접 일정
- 하위 일정 기반 프로젝트 자동 집계 Timeline
- 업무별 색상
- 일정 겹침 시 색상 혼합
- 일정 미정 상태
- Tree Drag & Drop으로 계층 변경
- 접기/펼치기
- 항목 활성/비활성
- Undo / Redo
- 자동 저장
- Zoom
- Excel Export
- Excel에서 계층을 실제 열/병합 셀로 표현

---

# 2. 핵심 설계 철학

## 2.1 Tree와 Timeline은 같은 데이터를 사용하지만 행 생성 규칙은 다르다.

왼쪽은 계층 구조를 표현한다.

오른쪽 Timeline은 실제 Bar가 있는 항목만 행으로 표현한다.

따라서 모든 Node가 Timeline 행을 갖는 것은 아니다.

## 2.2 프로젝트는 단순한 이름이 아니라 하위 항목을 묶는 Container다.

예:

마케팅
├── SNS
│   ├── 카드뉴스 기획
│   └── 카드뉴스 제작
├── 팝업 프로모션
│   ├── 메인 배너 제작
│   └── 설치 및 운영
└── 영상 콘텐츠
    ├── 영상 기획
    └── 영상 제작

프로젝트는 자기 하위 항목을 왼쪽에서 세로로 묶는다.

## 2.3 Timeline Bar가 있는 계층만 전용 Timeline 행을 가진다.

프로젝트 자체에 Bar가 없으면 프로젝트 전용 Timeline 행을 만들지 않는다.

프로젝트에 직접 일정 또는 자동 집계 Bar가 있으면, 해당 프로젝트가 묶고 있는 하위 영역의 맨 위에 전용 행을 하나 추가한다.

그 전용 행에는 하위 업무/프로젝트명이 다시 들어가지 않는다.

---

# 3. 데이터 모델

현재 코드의 WorkItem / Project 구조를 최대한 유지한다.

## 3.1 Node 개념

하나의 WorkItem/Node는 프로젝트 또는 업무가 될 수 있다.

권장 필드:

- id
- name
- parentId
- type: project | task
- startDate
- endDate
- isUndecided
- color
- active
- autoTimeline
- memo

### 주의

프로젝트도 직접 일정을 가질 수 있다.

따라서 프로젝트가 직접 일정 입력을 받을 수 있어야 한다.

프로젝트의 `autoTimeline = true`일 때만 직접 일정은 렌더링에서 무시하고 하위 일정 집계 Bar를 표시한다.

직접 일정 데이터 자체는 삭제하지 않는다.

---

# 4. 프로젝트 / 업무 속성

## 4.1 업무(Task)

업무는 실제 실행 단위다.

가능한 정보:

- 업무명
- 메모
- 시작일
- 종료일
- 일정 미정
- 색상
- 활성/비활성

담당자 열은 현재 버전에서 UI/Excel에서 제외한다.

담당자 데이터가 이미 존재하더라도 지금 단계에서는 삭제 여부를 임의로 결정하지 않는다. UI와 Excel에는 노출하지 않는다.

## 4.2 프로젝트(Project)

프로젝트는 하위 항목을 묶는 Container다.

프로젝트도 다음을 가질 수 있다.

- 프로젝트명
- 직접 시작일
- 직접 종료일
- 일정 미정
- 색상
- 활성/비활성
- 하위 일정 자동 반영
- 메모

단, 프로젝트가 프로젝트 단위로 사용될 때 담당자/메모 등 기존 업무 정보가 남아 있더라도 UI에서는 비활성/연한 상태로 처리한다는 기존 합의가 있었다.

최신 규칙상 프로젝트에 Timeline Bar가 존재하면 프로젝트 메모도 프로젝트 전용 행에 표시할 수 있다.

---

# 5. 프로젝트 직접 Timeline vs 자동 집계 Timeline

프로젝트는 두 가지 Timeline 표현 방식을 가진다.

## 5.1 자동 반영 OFF

프로젝트 자신의 직접 일정과 색상을 사용한다.

예:

마케팅 프로젝트  █████████████
  SNS            ████
  팝업              █████
  영상                  ███

## 5.2 자동 반영 ON

프로젝트 자신의 직접 일정과 색상은 렌더링에서 무시한다.

하위 업무/프로젝트의 유효 일정을 재귀적으로 집계하여 하나의 혼합 집계 Bar를 만든다.

예:

마케팅 프로젝트  🟦🟦🟪🟪🟩🟩
  SNS            🟦🟦🟦
  팝업                🟩🟩🟩
  영상                    🟨🟨

중요:

- 기존 직접 시작/종료일은 삭제하지 않는다.
- 자동 반영 OFF로 돌아가면 기존 직접 일정이 다시 보인다.
- 자동 반영 ON은 데이터 변경이 아니라 Timeline 표현 방식 변경이다.

---

# 6. 자동 집계 Timeline

## 6.1 집계 대상

자동 집계 시 다음을 사용한다.

- active 상태인 하위 항목
- 실제 날짜가 있는 하위 업무
- 하위 프로젝트의 유효 집계 결과

재귀적으로 모든 하위 계층을 탐색한다.

## 6.2 눈 OFF / active OFF

`active = false`는 단순 Timeline 숨김이 아니다.

해당 항목 자체를 비활성화한다.

효과:

- 해당 항목을 연하게 표시
- 해당 항목의 Timeline을 비활성화
- 상위 프로젝트 자동 집계에서 제외
- 하위 항목도 비활성 상태로 취급

단, 실제 데이터는 삭제하지 않는다.

## 6.3 접기와 active OFF는 다르다.

접기:

- 하위 항목을 화면에서 숨김
- 집계에는 영향 없음

active OFF:

- 해당 항목을 비활성화
- 집계에서 제외

---

# 7. 프로젝트 Timeline 행 규칙

## 7.1 프로젝트 Bar 없음

예:

마케팅
├── SNS
│   ├── 카드뉴스 기획
│   └── 카드뉴스 제작

마케팅 자체에 Bar가 없다면:

- 마케팅 전용 Timeline 행 없음
- 왼쪽 Tree에서는 마케팅이 하위 항목을 묶음
- Timeline에는 실제 Bar가 있는 하위 항목 행만 표시

## 7.2 프로젝트 Bar 있음

예:

마케팅 프로젝트에 직접 일정 또는 자동 집계 Bar가 있음.

그러면 하위 영역의 맨 위에 프로젝트 전용 행을 추가한다.

예:

A열       B열       C열       메모        Timeline

마케팅                       정산 필요    ███████
          SNS
                    카드뉴스 기획             ███
                    카드뉴스 제작  CI 확인    ████
          팝업
                    메인 배너 제작             ███

중요:

프로젝트 전용 행에는 하위 업무명이 들어가지 않는다.

---

# 8. 계층 구조

무제한 깊이를 지원한다.

예:

A 계층: 마케팅
B 계층: SNS
C 계층: 카드뉴스
D 계층: 카드뉴스 기획
E 계층: 세부 제작

웹에서는 Tree depth로 표현한다.

Excel에서는 depth가 열로 표현된다.

---

# 9. 항목 추가

`+ 항목 추가`의 기본 동작:

현재 선택된 항목의 하위 항목으로 생성.

예:

SNS 선택
→ + 항목 추가
→ SNS
   └ 새 항목

데이터상:

`newItem.parentId = selectedItem.id`

항목이 프로젝트인지 업무인지와 관계없이 같은 규칙을 사용한다.

---

# 10. Tree Drag & Drop

생성 후에는 Drag & Drop으로 계층을 변경할 수 있다.

## 10.1 항목 중앙에 Drop

해당 항목의 하위 항목으로 이동.

## 10.2 항목 위/아래에 Drop

해당 항목과 같은 계층의 형제로 이동.

## 10.3 왼쪽 영역으로 Drop

최상위 계층으로 이동.

데이터상:

`parentId = null`

즉 Excel 기준 A열 최상위 항목이 된다.

## 10.4 순환 참조 방지

자기 자신 또는 자기 자손을 자신의 부모로 지정할 수 없게 한다.

---

# 11. Timeline 날짜 구조

프로젝트 Timeline의 날짜 범위는 프로젝트에서 설정할 수 있다.

프로젝트:

- timelineStart
- timelineEnd

를 사용한다.

고정된 2026-09-01 ~ 2026-11-30을 하드코딩하지 않는다.

사용자가 Timeline 기간을 변경할 수 있어야 한다.

## 11.1 하루 단위 Grid

기본 DAY_WIDTH = 40px.

날짜 Header와 Grid는 동일한 날짜 배열을 사용한다.

Bar 좌표도 같은 하루 단위를 사용한다.

개념:

Bar 시작 위치 =
`getTimelineOffset(timelineStart, item.startDate) × DAY_WIDTH`

Bar 너비 =
`getTimelineDuration(item.startDate, item.endDate) × DAY_WIDTH`

---

# 12. Resize

Start / End Resize를 지원한다.

## 12.1 내부 Hitbox

Resize 판정 영역은 Bar 바깥으로 나가지 않는다.

기본:

- 왼쪽 내부 약 20px
- 오른쪽 내부 약 20px

DAY_WIDTH = 40px 기준.

짧은 Bar에서는 중앙 Move 영역이 확보되도록 동적으로 줄인다.

Resize는 Bar 내부에서만 판정한다.

## 12.2 최소 기간

Start Resize:

`startDate < endDate` 유지.

End Resize:

`endDate > startDate` 유지.

최소 1일 이상의 기간을 유지한다.

---

# 13. 일정 미정

모든 일정 입력 영역에 작은 `일정 미정` 옵션을 제공한다.

활성화하면:

- 시작일/종료일 입력 UI를 연하게 비활성화
- Timeline 전체 프로젝트 범위를 옅은 회색으로 채운 Bar 표시
- 업무/프로젝트가 일정 미정 상태임을 명확히 표시
- 메모가 비어 있다면 자동으로 `일정 미정` 입력
- 기존 메모가 있으면 기존 메모를 덮어쓰지 않는다.

예:

메모가 비어 있음
→ `일정 미정`

메모가 이미 있음
→ 기존 메모 유지

## 13.1 자동 집계와 일정 미정

권장 규칙:

일정 미정 항목은 프로젝트 자동 집계의 날짜 범위 계산에서 제외한다.

일정 미정은 개별 항목에서만 회색 Bar로 표현한다.

자동 집계 Bar에 임의의 날짜를 부여하지 않는다.

---

# 14. 색상

각 업무/프로젝트는 색상을 직접 선택할 수 있다.

## 14.1 기본 팔레트

우측 패널에서 기본 색상 팔레트를 제공한다.

사용자가 원하는 색을 선택한다.

## 14.2 사용자 색상 추가

팔레트의 `+` 버튼으로 사용자 지정 색상을 추가할 수 있다.

시스템이 업무에 색을 자동 배정하지 않는다.

## 14.3 프로젝트 색상

프로젝트도 직접 Timeline을 사용할 수 있으므로 색상을 가질 수 있다.

자동 반영 ON일 때는 프로젝트 자신의 색상을 렌더링에 사용하지 않는다.

---

# 15. 색상 혼합

자동 집계 Bar에서 여러 업무 일정이 겹치는 경우 시스템이 자동으로 혼합색을 계산한다.

예:

업무 A = 파랑
업무 B = 초록

겹치는 날짜:

A + B → 혼합색

A + B + C → 세 색의 혼합색

사용자가 혼합색을 직접 지정하지 않는다.

## 15.1 색상 계산

단순 RGB 평균보다는 OKLCH/지각 기반 색상 공간을 우선 검토한다.

목적은 물리적 물감 혼합을 재현하는 것이 아니라:

- 겹침을 시각적으로 표현
- 원래 색상 관계를 최대한 유지
- 밝기/채도 차이가 과도하게 왜곡되지 않게 함

구체적인 알고리즘은 UI Prototype에서 여러 방식으로 비교한 뒤 확정한다.

---

# 16. Timeline Zoom

Zoom을 지원한다.

예:

- 50%
- 75%
- 100%
- 150%
- 200%

또는 `- / +` 방식.

중요:

날짜 계산의 논리적 단위는 계속 1일이다.

화면 표시 폭만:

`displayDayWidth = DAY_WIDTH × zoom`

으로 변경한다.

Drag/Resize 계산과 Grid가 동일한 실제 표시 폭을 사용하도록 한다.

---

# 17. 접기 / 펼치기

프로젝트/하위 프로젝트에 접기/펼치기를 제공한다.

접으면:

- 하위 항목이 화면에서 숨겨진다.
- Timeline에서도 하위 행이 숨겨진다.

그러나:

- 자동 집계에는 영향을 주지 않는다.
- 데이터는 유지된다.

집계는 active 상태를 기준으로 한다.

---

# 18. 우측 상세 패널

항목을 클릭하면 우측 상세 패널이 열린다.

## 공통

- 이름
- 유형
- 색상
- 활성/비활성
- 일정 또는 일정 미정

## 업무

- 메모
- 시작일
- 종료일

## 프로젝트

- 직접 시작일
- 직접 종료일
- 메모
- 하위 일정 자동 반영

프로젝트가 업무로 사용되던 기존 데이터를 프로젝트로 전환했을 때, 프로젝트에서 사용하지 않는 업무 정보는 삭제하지 않고 UI에서 연하게 비활성화한다.

---

# 19. Active / 눈 ON/OFF

눈은 Timeline에 붙어 있는 기능이 아니다.

우측 상세 패널에서 항목 자체를 활성/비활성화한다.

예:

`항목 활성화 👁 ON`

OFF:

- 항목을 연하게 표시
- 해당 항목 일정 비활성
- 상위 자동 집계에서 제외

접기와 혼동하지 않는다.

---

# 20. Undo / Redo

지원한다.

- Cmd/Ctrl + Z
- Cmd/Ctrl + Shift + Z

대상:

- 날짜 변경
- Resize
- Drag 이동
- Tree 계층 이동
- 추가/삭제
- 색상 변경
- 메모 변경
- 활성/비활성 변경
- 일정 미정 변경
- 자동 반영 ON/OFF
- 프로젝트/업무 유형 변경

Undo/Redo와 자동 저장은 함께 설계한다.

---

# 21. 자동 저장

사용자가 별도 저장 버튼을 누르지 않아도 변경 사항을 자동 저장한다.

권장 저장소:

IndexedDB

저장 대상:

- Project
- WorkItem Tree
- 일정
- 색상
- 메모
- active
- autoTimeline
- isUndecided
- 접기 상태 등 Workspace 상태

저장 상태를 UI에서 간단히 표시할 수 있다.

예:

저장 중...
저장됨

---

# 22. Excel Export

Excel은 서비스의 현재 상태를 반영한 일정표다.

단순 데이터 Dump가 아니다.

## 22.1 열 구조

계층 깊이에 따라 열을 만든다.

예:

A = 1단계
B = 2단계
C = 3단계
...
마지막 계층 열
그 다음 = 메모
그 다음 = Timeline 날짜 열

예:

A: 마케팅
B: SNS
C: 카드뉴스 기획
D: 메모
E 이후: 날짜

## 22.2 병합

부모 항목은 자신의 하위 전체 행을 세로 병합한다.

예:

A2:A8 = 마케팅

B2:B3 = SNS
B4:B5 = 팝업 프로모션
B6:B7 = 영상 콘텐츠

## 22.3 프로젝트 전용 Timeline 행

프로젝트 Bar가 있으면 해당 프로젝트가 묶는 하위 영역의 맨 위에 전용 행을 추가한다.

예:

A열             B열              C열        메모        Timeline
마케팅                              정산 필요    ███████
                 SNS
                                  카드뉴스 기획             ███
                                  카드뉴스 제작  CI 확인     ████

전용 행에는 하위 업무명이 들어가지 않는다.

## 22.4 프로젝트 Bar 없음

프로젝트 전용 행을 만들지 않는다.

프로젝트 셀은 하위 항목을 묶기 위한 세로 병합만 한다.

## 22.5 담당자

현재 Excel에서는 담당자 열을 제외한다.

최종 기본 열:

`계층 열들 | 메모 | Timeline`

## 22.6 일정 미정

일정 미정 업무는 Excel에서도 옅은 회색 Bar로 표시한다.

메모가 자동 입력된 경우 `일정 미정`도 표시된다.

## 22.7 현재 화면 기준 Export

Excel Export는 사용자가 현재 보고 있는 상태를 기준으로 한다.

특히 접힌 항목은 Export에서 제외한다.

즉:

- 현재 펼쳐져 보이는 계층만 Export
- 현재 표시되는 Timeline 행만 Export
- 현재 active 상태 반영
- 현재 색상 반영
- 현재 메모 반영
- 현재 자동 집계 결과 반영

---

# 23. Excel Timeline 색상

업무별 지정 색상을 Excel 셀 배경색으로 적용한다.

자동 집계 Bar는 서비스에서 계산된 최종 색상을 그대로 Excel 셀에 적용한다.

Excel에서 다시 색상 혼합을 계산할 필요는 없다.

---

# 24. 현재 화면과 Excel의 차이

서비스:

- 인터랙티브
- Drag & Drop
- Resize
- Zoom
- 접기/펼치기
- 활성/비활성
- 자동 집계
- 색상 혼합

Excel:

- Export 시점의 결과물
- 정적인 일정표
- 셀 병합
- 셀 배경색
- 메모
- 날짜 Grid

---

# 25. 삭제 규칙

부모 항목 삭제 시 하위 항목을 어떻게 할지 명확히 구현해야 한다.

권장:

- 부모 + 모든 자손을 함께 삭제
- 삭제 전 확인

예:

`SNS 및 하위 4개 항목을 모두 삭제하시겠습니까?`

Undo로 복구 가능하게 한다.

---

# 26. 구현 아키텍처 권장

기존 코드를 전체적으로 갈아엎지 않는다.

현재 구조를 최대한 유지한다.

권장 분리:

- `project.ts`: 데이터 타입
- `date-utils.ts`: 날짜 계산
- `tree-utils.ts`: Tree 재귀/계층 계산
- `timeline-utils.ts`: Bar 계산/집계/색상 혼합
- `excel-export.ts`: Excel Export
- `page.tsx`: Workspace UI 및 상태 연결

단, 실제 현재 파일 구조를 먼저 확인하고 필요 이상의 파일 분리는 하지 않는다.

---

# 27. 핵심 계산 유틸

Tree:

- `getChildren`
- `getDescendants`
- `getDepth`
- `buildVisibleTree`
- `moveNode`
- `canMoveNode`

Timeline:

- `getTimelineDates`
- `getTimelineOffset`
- `getTimelineDuration`
- `getEffectiveTimeline`
- `getAggregatedTimeline`
- `getOverlapSegments`
- `blendColors`

---

# 28. 개발 시 반드시 지켜야 할 원칙

1. 기존 기능을 전체 재작성하지 않는다.
2. 기존 WorkItem 데이터 구조와 호환성을 유지한다.
3. 날짜 계산은 하루 단위를 기준으로 한다.
4. Grid와 Bar는 동일한 좌표계를 사용한다.
5. 자동 집계는 원본 날짜를 덮어쓰지 않는다.
6. 접기와 active OFF를 혼동하지 않는다.
7. 프로젝트 전용 Bar 행과 하위 업무 행을 분리한다.
8. 프로젝트 전용 행에는 하위 업무명이 중복 표시되지 않는다.
9. Excel의 계층은 depth별 열로 표현한다.
10. 부모 셀은 자식 전체 행을 세로 병합한다.
11. 현재 화면의 상태를 Excel Export에 반영한다.
12. 기능 구현 전에 기존 코드를 읽고 현재 구조와 충돌 여부를 확인한다.
13. 불필요한 라이브러리와 파일을 추가하지 않는다.
14. 임의의 UX 규칙을 추가하지 않는다.
15. 기존 합의와 충돌하는 경우 먼저 사용자에게 확인한다.

---

# 29. 구현 우선순위

## Phase 1 — 데이터 / Tree

1. Node type 확장
2. parentId 기반 무제한 Tree
3. depth 계산
4. 항목 추가
5. Drag & Drop 계층 이동
6. 최상위 Drop
7. 순환 참조 방지

## Phase 2 — Timeline

1. Project Timeline 범위
2. 직접 일정
3. 일정 미정
4. Bar 행 생성 규칙
5. Start/End Resize
6. 프로젝트 직접 Bar
7. 자동 집계 Bar

## Phase 3 — 상태 / 색상

1. active ON/OFF
2. 색상 팔레트
3. 사용자 색상 추가
4. 자동 색상 혼합
5. 접기/펼치기

## Phase 4 — Workspace

1. Undo/Redo
2. IndexedDB 자동 저장
3. 저장 상태
4. Zoom

## Phase 5 — Excel

1. depth별 열 생성
2. 부모 셀 병합
3. 프로젝트 전용 행 생성
4. 메모
5. Timeline 셀
6. 색상
7. 일정 미정
8. 현재 화면 상태 기준 Export

---

# 30. 최종 예시

## 웹 Workspace 개념

```text
A: 마케팅
│
├── B: SNS
│   ├── C: 카드뉴스 기획     █████
│   └── C: 카드뉴스 제작       █████
│
├── B: 팝업 프로모션
│   ├── C: 메인 배너 제작        ███
│   └── C: 설치 및 운영            █████
│
└── B: 영상 콘텐츠
    ├── C: 영상 기획                  ███
    └── C: 영상 제작                    █████
```

마케팅 자체에 Bar가 있다면:

```text
A: 마케팅                 정산 필요       🟪🟪🟪🟪🟪
│
├── B: SNS
│   ├── C: 카드뉴스 기획                    🟦🟦🟦
│   └── C: 카드뉴스 제작                      🟦🟦🟦
│
└── B: 팝업
    ├── C: 메인 배너                        🟩🟩
    └── C: 설치 운영                           🟩🟩🟩
```

마케팅 Bar가 없으면 첫 번째 `마케팅` Timeline 전용 행은 없다.

---

# 31. 개발 인수인계 메시지

Claude Code에 이 문서를 먼저 읽힌 후 다음 원칙으로 작업한다.

> 이 프로젝트는 기존 Timeline Workspace의 후속 개발이다.
>
> 먼저 현재 repository의 파일 구조와 기존 구현을 읽어라.
>
> 이 문서의 요구사항을 기준으로 현재 구현과의 차이를 분석하라.
>
> 기존 코드를 무조건 재작성하지 말고 최소 변경을 우선하라.
>
> 특히 `Project / WorkItem / parentId / Timeline 계산 / Drag / Resize`의 기존 구현을 먼저 확인하라.
>
> 구현 전에:
> 1. 현재 구조
> 2. 변경이 필요한 파일
> 3. 각 파일의 변경 내용
> 4. 기존 기능에 미칠 영향
> 5. 잠재적인 충돌
>
> 을 먼저 설명하라.
>
> 사용자의 확인 없이 대규모 코드를 한 번에 변경하지 마라.
>
> 구현 후에는 TypeScript 타입 검사와 lint를 실행하고, 변경 사항과 테스트 결과를 요약하라.

---

# 32. 현재 설계에서 의도적으로 제외된 기능

다음은 현재 범위가 아니다.

- 담당자 컬럼
- 자동 색상 배정
- Google Sheets 연동
- Excel 양방향 동기화
- 서버 기반 다중 사용자 협업
- 실시간 공동 편집
- 프로젝트 자동 날짜 확장
- 별도 프로젝트 생성 페이지
- Timeline 범위 자동 변경
- 별도 저장 버튼 중심의 저장 방식

추후 필요할 경우 별도 Phase로 추가한다.

---

# 33. 최종 핵심 문장

이 서비스의 핵심은:

> **계층 구조를 왼쪽에서 세로 병합으로 표현하고, 실제 일정이 있는 Node만 Timeline 행을 가지며, 프로젝트가 자체 Timeline을 가지면 자기 하위 영역의 최상단에 전용 Timeline 행을 추가하고, 자동 반영을 켜면 하위 일정의 색상과 기간을 재귀적으로 집계하여 하나의 혼합 Timeline Bar로 보여주는 계층형 Timeline Workspace다.**

Excel에서는:

> **계층 깊이를 열로 표현하고, 부모 셀은 자신의 하위 전체 행을 세로 병합하며, 프로젝트 Timeline이 있는 경우 해당 계층의 맨 위에 프로젝트 전용 행을 추가한다.**

이 두 문장을 전체 제품 구현의 최상위 원칙으로 사용한다.
